name: Deploy to IIS Server

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'

    - name: Install dependencies
      run: npm install

    - name: Build React app
      run: npm run build

    - name: Deploy to IIS
      uses: appleboy/rsync-action@master
      with:
        args: -avz --delete
        remote_host: 13.50.232.115
        remote_user: Administrator
        remote_password: cyOV@lnZc@Jh-;u1bV8aJ.c8lIdBYhNp
        source: ./build/
        destination: "C:/inetpub/wwwroot/react"
